spring:
  datasource:
    url: jdbc:mysql://localhost:3306/task_management
    username: root
    password: 11752037466
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

#  security:
 #   user:
  #    name: admin
   #   password: password

# server:
#  servlet:
#    context-path: /yeni-context-path

